# Инструкция по установке и запуску проекта

## Требования

- Компьютер с Windows, macOS или Linux
- Доступ в интернет
- VSCode (уже установлен)
- Node.js версии 16 или выше

---

## Шаг 1: Проверка и установка Node.js

### Проверка наличия Node.js

1. Откройте **Terminal** (в VSCode: меню `Terminal` → `New Terminal`)
2. Выполните команду:
   ```bash
   node --version
   ```

### Если Node.js НЕ установлен:

#### На macOS:
```bash
# Установка через Homebrew (если есть)
brew install node

# Или скачайте установщик:
```
Перейдите на https://nodejs.org/en/download/ → скачайте LTS версию для macOS → запустите установщик

#### На Windows:
1. Перейдите на https://nodejs.org/en/download/
2. Скачайте **LTS версию** для Windows
3. Запустите установщик `.msi`
4. Следуйте инструкциям (оставьте все галочки по умолчанию)
5. Перезапустите VSCode после установки

#### На Linux (Ubuntu/Debian):
```bash
curl -fsSL https://deb.nodesource.com/setup_lts.x | sudo -E bash -
sudo apt-get install -y nodejs
```

### Проверка после установки:
```bash
node --version   # Должно показать v18.x.x или v20.x.x
npm --version    # Должно показать 9.x.x или 10.x.x
```

---

## Шаг 2: Распаковка проекта

1. **Найдите файл** `feedback-proxy.zip` на вашем компьютере
2. **Распакуйте** его в удобное место, например:
   - macOS: `/Users/ИМЯ_ПОЛЬЗОВАТЕЛЯ/Documents/feedback-proxy`
   - Windows: `C:\Users\ИМЯ_ПОЛЬЗОВАТЕЛЯ\Documents\feedback-proxy`
   - Linux: `/home/ИМЯ_ПОЛЬЗОВАТЕЛЯ/feedback-proxy`

3. **Откройте папку в VSCode**:
   - Запустите VSCode
   - Меню: `File` → `Open Folder...`
   - Выберите папку `feedback-proxy`

---

## Шаг 3: Установка зависимостей

1. Откройте **Terminal** в VSCode (`Terminal` → `New Terminal`)

2. Убедитесь, что вы находитесь в папке проекта:
   ```bash
   pwd  # macOS/Linux
   cd   # Windows
   ```
   Должно показать путь к папке `feedback-proxy`

3. Установите зависимости:
   ```bash
   npm install
   ```

4. Дождитесь завершения (может занять 1-2 минуты)

5. Проверьте, что появилась папка `node_modules`:
   ```bash
   ls -la  # macOS/Linux
   dir     # Windows
   ```

---

## Шаг 4: Настройка Google Apps Script

### 4.1. Создание/открытие Google Sheets

1. Откройте вашу Google Sheets таблицу (или создайте новую на https://sheets.google.com)
2. Запомните/скопируйте **ID таблицы** из URL:
   ```
   https://docs.google.com/spreadsheets/d/1xLdjIhse1Gl4dET7SCUgQhrpp06WhWFLEJdEGrPUekg/edit
                                          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
                                          Это ID таблицы
   ```

### 4.2. Создание Apps Script

1. В Google Sheets: **Extensions** → **Apps Script**
2. **Удалите** весь код в редакторе
3. **Откройте файл** `google-apps-script.js` в VSCode
4. **Скопируйте** весь код из файла
5. **Вставьте** в редактор Apps Script
6. **Измените** ID таблицы на строке 8:
   ```javascript
   const SPREADSHEET_ID = 'ВСТАВЬТЕ_СЮДА_ВАШ_ID_ТАБЛИЦЫ';
   ```
7. Нажмите **Ctrl+S** (или **Cmd+S** на macOS) чтобы сохранить

### 4.3. Деплой Web App

1. В редакторе Apps Script нажмите **Deploy** → **New deployment**
2. Нажмите на **шестерёнку** рядом с "Select type"
3. Выберите **Web app**
4. Заполните настройки:
   - **Description:** `Feedback API` (опционально)
   - **Execute as:** **Me (ваш email)**
   - **Who has access:** **Anyone** ⚠️ КРИТИЧЕСКИ ВАЖНО!
5. Нажмите **Deploy**
6. Появится окно авторизации:
   - Нажмите **Authorize access**
   - Выберите ваш Google аккаунт
   - Нажмите **Advanced** → **Go to "Untitled project" (unsafe)**
   - Нажмите **Allow**
7. **Скопируйте Web app URL** (будет вида `https://script.google.com/macros/s/AKfyc.../exec`)

---

## Шаг 5: Настройка переменной окружения

### Вариант А: Создание .env файла (рекомендуется)

1. В VSCode откройте файл `.env.example`
2. Создайте новый файл: `File` → `New File` → сохраните как `.env` (в корне проекта)
3. Добавьте в `.env`:
   ```
   GAS_URL=https://script.google.com/macros/s/ВАША_ССЫЛКА_ИЗ_ШАГА_4/exec
   PORT=3000
   HOST=127.0.0.1
   ```
4. **Замените** `ВАША_ССЫЛКА_ИЗ_ШАГА_4` на реальный URL из шага 4.3

### Вариант Б: Без .env файла

Если не хотите создавать `.env`, используйте команду с переменной при запуске (см. Шаг 6, вариант Б)

---

## Шаг 6: Запуск сервера

### Вариант А: Если создали .env файл

В терминале VSCode выполните:
```bash
npm start
```

### Вариант Б: Без .env файла

#### На macOS/Linux:
```bash
GAS_URL='https://script.google.com/macros/s/ВАША_ССЫЛКА/exec' npm start
```

#### На Windows (PowerShell):
```powershell
$env:GAS_URL='https://script.google.com/macros/s/ВАША_ССЫЛКА/exec'; npm start
```

#### На Windows (CMD):
```cmd
set GAS_URL=https://script.google.com/macros/s/ВАША_ССЫЛКА/exec && npm start
```

### Ожидаемый результат:

Вы должны увидеть:
```
> feedback-proxy@1.0.0 start
> node server.js

Proxy server running at http://127.0.0.1:3000
```

⚠️ **НЕ ЗАКРЫВАЙТЕ** это окно терминала! Сервер должен работать постоянно.

---

## Шаг 7: Тестирование в браузере

1. Откройте браузер (Chrome, Firefox, Safari)
2. Перейдите по адресу: **http://localhost:3000**
3. Вы должны увидеть форму оценки обслуживания

### Тестирование формы:

1. Выберите **кассу** (1, 2 или 3)
2. Нажмите на **эмодзи** для оценки (от 😃 до 😡)
3. Если выбрали 😡 или 🙁, появится поле для комментария
4. Нажмите **"Отправить"**
5. Должно появиться сообщение **"Спасибо за ваш отзыв!"**

### Проверка в Google Sheets:

1. Откройте вашу Google таблицу
2. Должен появиться лист **"Отзывы"** с колонками:
   - `timestamp` - время в формате DD.MM.YYYY HH:mm:ss
   - `cashdesk` - номер кассы
   - `rating` - оценка (1-5)
   - `comment` - комментарий (если был)

---

## Шаг 8: Остановка сервера

Когда нужно остановить сервер:

1. Перейдите в окно терминала где запущен сервер
2. Нажмите **Ctrl+C** (на macOS тоже Ctrl+C, не Cmd+C)
3. Сервер остановится

---

## Возможные проблемы и решения

### Проблема: "node: command not found"
**Решение:** Node.js не установлен. Вернитесь к Шагу 1.

### Проблема: "npm: command not found"
**Решение:** Перезапустите VSCode после установки Node.js.

### Проблема: "Port 3000 is already in use"
**Решение 1:** Измените порт в `.env`:
```
PORT=3001
```
И в файле `feed.html` измените строку 73:
```javascript
fetch('http://localhost:3001/feedback', {
```

**Решение 2:** Найдите и убейте процесс на порту 3000:
```bash
# macOS/Linux
lsof -ti :3000 | xargs kill -9

# Windows PowerShell
Get-Process -Id (Get-NetTCPConnection -LocalPort 3000).OwningProcess | Stop-Process -Force
```

### Проблема: "Не удалось отправить отзыв" в браузере
**Причины:**
1. Сервер не запущен → запустите `npm start`
2. Неправильный GAS_URL → проверьте Шаг 5
3. Google Apps Script настроен неправильно → проверьте Шаг 4.3, особенно "Who has access: Anyone"

### Проблема: HTTP 401 или 403 от Google
**Решение:**
1. Откройте Apps Script: **Deploy → Manage deployments**
2. Нажмите карандаш (Edit) на вашем деплое
3. Убедитесь что **"Who has access" = "Anyone"**
4. Нажмите **Update**

### Проблема: Данные не появляются в Google Sheets
**Проверьте:**
1. Правильный ли SPREADSHEET_ID в `google-apps-script.js`
2. Есть ли у вас доступ к таблице (откройте в браузере)
3. Посмотрите ошибки в терминале где запущен сервер

---

## Полезные команды

### Проверка статуса
```bash
# Проверить запущен ли сервер
curl http://localhost:3000/health
# Должен вернуть: {"status":"ok"}
```

### Тестирование API напрямую
```bash
# macOS/Linux
curl -X POST http://localhost:3000/feedback \
  -H 'Content-Type: application/json' \
  -d '{"cashdesk":"1","rating":5,"comment":"Test"}'

# Windows PowerShell
Invoke-RestMethod -Uri http://localhost:3000/feedback -Method POST -ContentType 'application/json' -Body '{"cashdesk":"1","rating":5,"comment":"Test"}'
```

### Просмотр логов
Все логи выводятся в терминал где запущен сервер. Смотрите:
- `[Feedback] Received data:` - получены данные от клиента
- `[Feedback] GAS response status:` - ответ от Google

---

## Контрольный чек-лист

- [ ] Node.js установлен и работает (`node --version`)
- [ ] Проект распакован в удобное место
- [ ] Выполнена команда `npm install`
- [ ] Папка `node_modules` создана
- [ ] Google Apps Script создан и задеплоен
- [ ] Web App URL скопирован
- [ ] Файл `.env` создан с правильным GAS_URL
- [ ] Сервер запущен (`npm start`)
- [ ] В терминале видно "Proxy server running at..."
- [ ] Браузер открывает http://localhost:3000
- [ ] Форма отображается корректно
- [ ] Тестовый отзыв отправляется успешно
- [ ] Данные появляются в Google Sheets

---

## Дополнительная информация

### Структура проекта:
```
feedback-proxy/
├── server.js                 # Node.js сервер
├── feed.html                # HTML форма
├── google-apps-script.js    # Код для Google Apps Script
├── package.json             # Зависимости проекта
├── .env.example             # Пример конфигурации
├── .gitignore              # Игнорируемые файлы
├── README.md               # Основная документация
├── INSTALLATION.md         # Эта инструкция
└── node_modules/           # Зависимости (создается npm install)
```

### Порты:
- **3000** - основной порт приложения
- Если занят, можно использовать 3001, 8080, 8000

### Безопасность:
- Файл `.env` содержит секреты - НЕ публикуйте его
- GAS_URL можно считать публичным (он уже доступен в браузере)

---

## Контакты для помощи

Если возникли проблемы:
1. Проверьте раздел "Возможные проблемы и решения"
2. Посмотрите логи в терминале
3. Проверьте консоль браузера (F12 → Console)

**GitHub репозиторий:** https://github.com/bwgit234-creator/git-remote-add-origin-https-github.com-feedback-feedback-proxy
